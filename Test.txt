# Calcul du pourcentage de spam
    spam_percentage = (nb_spam / total_rows) * 100 if total_rows > 0 else 0
    spam_text = f"Le nombre de spams reçus est {spam_percentage:.2f}% du total des requêtes."
    
